# TODO — Organisatorische Vorbereitungen

Aufgaben, die reale Konten/Zugänge brauchen und die reine Konzept-Doku (`idea/`, `pipeline/`) nicht abdeckt — sortiert nach Dringlichkeit. Fachliche/technische Punkte, die eine Entwicklungs-Session selbst abarbeiten kann, stehen in `TODO-SESSIONS.md`.

## Vor dem Produktivbetrieb (Putzdienst als erste Fachdomäne, `fachdomaenen.md` Abschnitt 7)

- [x] DNS A/AAAA-Record für `api.clemens.schule` beim DNS-Provider der Schule (All-Inkl, KAS-Panel) auf die Server-IP setzen — `pipeline/runbook.md` Schritt 2, Voraussetzung für Caddys automatisches HTTPS und damit für den externen Zugriffsweg
- [x] Entra-ID-App-Registrierung im M365-Tenant der Schule anlegen (Tenant-Admin-Zugriff nötig, Tenant-Restriktion) — `pipeline/runbook.md` Schritt 5, zwingend da die Verwaltung den Putzdienst-Prozess intern startet und die Termine pflegt
- [x] DNS A/AAAA-Records für `portal.clemens.schule` (Eltern) und `intern.clemens.schule` (Personal) auf dieselbe Server-IP setzen — `pipeline/runbook.md` Schritt 2; ohne sie holt Caddy für die beiden Oberflächen kein Zertifikat und der Elternzugang hat keine Adresse
- [x] Redirect-URI `https://intern.clemens.schule` in der Entra-ID-App-Registrierung eingetragen, Plattform „Single-page application“ — sie zeigt auf die Origin des internen Frontends, nicht auf die API (`idea/04-identitaet-zugriff.md`, `project-parts.md` Abschnitt 9). Es ist die einzige der Registrierung: die API verifiziert nur und holt selbst kein Token, der Graph-Zugriff läuft App-only, und der Elternzugang läuft über den eigenen OTP-Weg. Front-channel-Logout bleibt leer — für Personal gibt es keine serverseitige Sitzung, die ein Abmelden räumen müsste; die Implicit-Grant-Haken bleiben aus, MSAL nutzt Authorization Code mit PKCE
- [x] **Die echten Tenant- und Audience-Werte der App-Registrierung stehen im Stack** — nicht als Handeintrag in der `.env` auf der VPS: die schreibt der Playbook-Lauf aus `wb-vps/ansible/roles/podman_rootful/templates/app-stack.env.j2`, und die Werte selbst liegen als `wb_jwt_tenant_id`/`wb_jwt_audience` in `wb-vps/ansible/group_vars/all.yml`. Wer sie auf dem Host ändert, verliert die Änderung beim nächsten Lauf. Keine Geheimnisse, sondern öffentliche App-Konfiguration, deshalb weder Secrets-Datei noch `.env`-Pflege von Hand. Compose reicht sie als `JWT_TENANT_ID`/`JWT_AUDIENCE` ohne Präfix in den Container — die `WB_`-Namen sind Compose-Variablen, keine Settings-Felder
- [ ] **Rollen-Claim und Audience-Form an einem echten Token bestätigen** (Tenant-Admin-Zugriff nötig) — zwei Fragen, ein Blick in jwt.ms. Erstens liest `wb-backend/app/core/security.py` den Claim `roles`; ob die App-Registrierung ihn so oder als Gruppen-Claim ausstellt, sieht man allein am ausgestellten Token, und fällt er anders aus, ist es eine Zeile in `CurrentUser`. Zweitens trägt `wb_jwt_audience` heute die nackte Client-GUID, das `aud` eines v2-Tokens kann aber `api://<guid>` lauten, sobald das Frontend den eigenen Scope anfordert (`wb-vps/ansible/group_vars/all.yml` hält beides fest). Beides muss vor dem ersten geschützten Endpunkt stimmen; falsch geraten antwortet jeder Personal-Login mit 401
- [x] Application Access Policy auf `post@clemens.schule` gesetzt: mail-enabled Security Group `wb-mailsender@wsclem.onmicrosoft.com` mit genau diesem Postfach, Policy darauf. Gegengeprüft mit `Test-ApplicationAccessPolicy` — `Granted` für das Absenderpostfach, `Denied` für ein fremdes; die Zusage ist damit gemessen und nicht behauptet — ohne sie darf das App-only-`Mail.Send` im Namen jedes Postfachs im Tenant senden (`idea/04-identitaet-zugriff.md`). Trägt OTP-Versand, Buchungsbestätigung, Erinnerungen und Rundmail, muss damit vor dem Putzdienst-Buchungsfenster stehen
- [ ] NAS-Backup-Bootstrap auf der Synology (DSM-Zugriff nötig): SSH-Keypair für den Pull-Key generieren (privat ausschließlich auf dem NAS), Task-Scheduler-Job anlegen, VPS-Host-Key vorab in `known_hosts` pinnen (nach jedem `rebuild.sh` neu zu wiederholen) — `idea/05-backup-recovery.md`, muss vor den ersten echten Elterndaten laufen. Öffentlichen Pull-Key liefert der zweite Admin nach seiner Urlaubsrückkehr Ende August 2026
- [x] Neue Secrets im gemeinsamen KeePass ablegen: DB-Rollen-Passwörter (Runtime/Migration/Backup), Entra-ID Client-ID/Tenant-ID/Client-Secret, `age`-Verschlüsselungs-Passphrase — Feldschema folgt mit der `secrets.example.env`-Erweiterung im App-Stack-Repo
- [x] MFA auf dem healthchecks.io-Konto aktivieren (TOTP, Kontoeinstellungen) — `rules.md` Abschnitt 2; ohne das schaltet ein übernommenes Konto den Dead-Man's-Switch stumm, und Backup-Fehlschlag/Disk-Voll/fehlgeschlagener Patch-Lauf melden ausschließlich dorthin
- [x] **DMARC steht auf `p=quarantine`** (DNS-Provider der Schule, All-Inkl KAS-Panel) — `_dmarc.clemens.schule` trägt `v=DMARC1;p=quarantine`, gegengeprüft mit `dig +short TXT _dmarc.clemens.schule`. SPF und DKIM standen bereits; die Policy war der letzte weiche Punkt, und United Internet (GMX, web.de — zusammen der größte Teil der Elternpostfächer) bewertet sie: unter `p=none` blieb eine Code-Mail an eine GMX-Adresse im Message Trace auf `Pending` hängen, dieselbe Mail an ein Tenant-Postfach kam sofort an. Der Anmeldecode und damit der gesamte Elternzugang hängen nicht mehr daran. Offen bleibt allein der Schritt auf `p=reject`, sobald die `rua`-Berichte sauber sind
- [ ] Stripe-Konto anlegen und AVV abschließen (stellt Stripe im Dashboard bereit) — trägt den Putzdienst-Freikauf und damit die erste Q3-Zahlung (`grenzkarte.md`), muss vor den ersten echten Zahlungen stehen. Dabei zwei Punkte klären: welche Stripe-Gesellschaft Vertragspartner ist (EU-Sitz vs. Drittland-Transfer nach Art. 44 ff.) und welche Personendaten übertragen werden — Betrag und Referenz sind unvermeidlich, ein Name wird nicht übertragen, und die Mailadresse tippt der Elternteil auf Stripes Bezahlseite selbst ein, damit Stripe den Beleg schickt (`api/gemeinsam.md`, `idea/06-dsgvo-organisatorisch.md`). Dazu im Dashboard den Belegversand für erfolgreiche Zahlungen einschalten — eine Einstellung, kein Code, und im Testmodus verschickt Stripe keine, der Pfad ist also erst am echten Konto zu prüfen
- [ ] Zweiter Admin: GitHub-Username + SSH-Key einholen, vollen Zugriff auf GitHub-Org und VPS einrichten — für die Entwicklung unkritisch (alles in Git reproduzierbar), muss aber vor dem Produktivbetrieb mit echten Elterndaten stehen (`rules.md` Abschnitt 6). SSH-Key zugesagt für die Urlaubsrückkehr Ende August 2026
- [x] Vis365-Feldliste beim zweiten Admin holen: Vis365 → Schulverwaltungsimport → ASV-BW, die Anleitung stammt von ihm. Definiert, welche Felder der M365-Kontenexport braucht — zweite reale Datenquelle für das Stammdaten-Schema neben den Voranmeldeformularen (`schema/stammdaten-schema.sql`). Blockiert den Schema-Entwurf nicht, muss aber vor der Abnahme abgeglichen sein
- [ ] Stammdaten-Export für den Vollimport beschaffen (der Stichtag, ab dem der Freeze gilt, `grenzkarte.md`). **Liegt beim Betreiber selbst**, keine Zulieferung durch Dritte und damit keine offene Fremdabhängigkeit — steht hier nur, damit die Frage „wer erzeugt diesen Export" nicht erneut als Blocker aufgemacht wird. Einzige Randbedingung: die Schema-Durchsicht unten liegt davor, sonst friert der Import einen ungeprüften Stand ein
- [ ] Stammdaten-Schema gemeinsam mit dem zweiten Admin durchgehen (von ihm angeboten) — nach seiner Urlaubsrückkehr Ende August 2026, vor dem ersten Import echter Daten. Läuft gegen den eingefrorenen Stand (`grenzkarte.md`, „Freeze der Stammdaten") und ist damit die **letzte** Gelegenheit, eine bestehende Spalte noch zu ändern — danach ist jede Änderung eine Migration auf echten Personendaten
- [x] Klären, ob ASV-BW oder Optigem eine BIC brauchen — **Optigem verlangt sie für Konten außerhalb Deutschlands**, `sepa_mandates.bic` bleibt damit und ist nicht streichbar (`schema/stammdaten-schema.sql`)
- [x] Ist das SEPA-Mandat an den Zahler oder an das Kind gebunden — **an das Kind**, je Kind wird ausdrücklich eines eingesammelt. Im Schema vor dem Freeze in die eigene Tabelle `sepa_mandates` je Kind verlagert (`schema/stammdaten-schema.sql`)
- [ ] **Zweck der Voranmeldefelder Beruf, Konfession, Staatsangehörigkeit und Kirchengemeinde intern beschließen** (Schulleitung, Datenschutzbeauftragte:r). Die Spalten stehen inzwischen an `children` und `guardians` — entschieden, weil ein `DROP COLUMN` billig ist und ein beim Vollimport nicht erhobener Wert nicht nacherhebbar (`prozesse.md` Abschnitt 3.3). Damit ist die Aufgabe kleiner, aber nicht erledigt: Konfession ist ein Art.-9-Datum, und ein Feld ohne beschlossenen Zweck mit echten Personendaten zu füllen, schließt `rules.md` Abschnitt 7 aus. Die genannten Zwecke (Elternmitarbeit, christliche Familie, Wurzeln des Kindes) sind belastbar zu bestätigen, sonst fällt das Feld vor dem Import wieder raus. **Weiterhin vor dem Vollimport, nicht danach**
- [ ] **Die Warteliste vor dem Import durch das Sekretariat bestätigen lassen** — ihr heutiger Stand ist nicht belastbar: Einträge können längst erledigt, abgesagt oder eingeschult sein (`prozesse.md` Abschnitt 6). Ungeprüft übernommen entsteht ein Bestand, dem man den Verfall nicht ansieht, und die jährliche Fortschreibung zieht ihn still weiter. Alternative: mit Status „ungeprüft" übernehmen (`TODO-SESSIONS.md`, `soll-prozesse/07-aufnahmeentscheidung.md`)
- [ ] **Die Schuladressen aus dem Tenant beschaffen**, je bestehendem Schüler und Mitarbeitendem (zweiter Admin, Tenant-Zugriff) — ohne sie hat jede Klassenliste eine leere Spalte, und kein Mitarbeitender kommt an sein Konto (`soll-prozesse/README.md`, `soll-prozesse/13-m365-konten.md`)

- [x] Benennen, **wer** den Erlass der Putzdienst-Pflicht und die Aussetzung der 45-€-Strafe auslösen darf — **Geschäftsführung und Schulleitung**, sonst niemand und nach außen unsichtbar (`schema/putzdienst-schema.sql`). Erster konkret benannter Fall der Rollendifferenzierung innerhalb des internen Personals; offen bleibt allein die Umsetzung als Spalten-GRANT bei der Rollenvergabe im Backend

## Unabhängig vom Putzdienst-Termin

- [x] Mensa-Anmeldeformular beim Sekretariat bzw. der Hausdienstverwaltung anfordern — **liegt vor** (`~/Downloads/25_26_RS_Mittagessen.pdf`, Realschule 25/26); Feldliste und Regeln ausgewertet in `prozesse.md` Abschnitt 9. Offen bleibt, ob ein Grundschul-Pendant existiert — hier und sonst nirgends geführt
- [x] `wb-backend`-Grundgerüst gegen `wb-backend/CLAUDE.md` durchgesehen und nachgezogen — die Funde und ihre Auflösung stehen im Repo selbst (`CLAUDE.md` §5/§6, `README.md`, `tests/test_privileges.py`); was daraus für die Domänen folgt, in `TODO-SESSIONS.md`
- [x] Wer den Dublettenhinweis an einer eingehenden Anmeldung abarbeitet („Verdacht auf vorhandenes Kind", `TODO-SESSIONS.md`): **das Sekretariat**. Der Hinweis hängt bewusst an der Bewerbung/Buchung, die ohnehin gesichtet wird, und nicht an einer eigenen Liste — er begleitet den Vorgang damit, statt ihn anzuhalten
- [x] Klären, ob §20 Abs. 9 IfSG eine Meldung ans Gesundheitsamt verlangt, wenn kein Masernnachweis vorgelegt wird — **ja**: die Infektionsschutz-Anlage des Betreuungsvertrags hält die Meldung ausdrücklich fest. Das Schema hält weiterhin nur fest, **ob und wie** der Nachweis vorlag (`measles_proofs` samt der Werteliste `measles_presentation_types`); die erfolgte Meldung trägt keine Spalte, sondern eine Q5-Aufgabe beim Sekretariat — der Meldefall ist genau der ohne Nachweiszeile, an der eine Spalte hängen könnte (`soll-prozesse/09-hortvertrag.md`, `schema/gesundheit-schema.sql`)
- [ ] Nach dem Produktivstart klären, ob der **Hort eigenen Dateizugriff** bekommt (Hortleitung, Sekretariat) — vorerst führt er nichts Eigenes neben Hortverträgen und Ferienprogramm, seine Gesundheitsangaben bekommt er als Zeilen aus Weltenbaum (`grenzkarte.md`, Q2)
- [ ] **Drei Vertragstexte anpassen** (Geschäftsführung) — jeder ist Vorbedingung für seinen Prozess, nicht Beiwerk: Solange der Text die alte Mechanik beschreibt, kann der digitale Ablauf nicht laufen, ohne dem Unterschriebenen zu widersprechen.
  - **Betreuungsvertrag**, drei Anpassungen: Ende zum Ende der Klasse 4 bzw. 5 **ohne Kündigung**, die Schriftform, und die dritte im Block (`soll-prozesse/09-hortvertrag.md`, `schema/anmeldung-schema.sql`) — vor Domäne 2/4
  - **Essensbedingungen**, zwei Anpassungen: Anmeldung und Kündigung **im Portal statt mit Unterschrift**, dazu die Lastschrift-Ermächtigung (`soll-prozesse/11-mensa.md`, `schema/mensa-schema.sql`) — vor dem Mensa-Abo im Oktober
  - **Anlage zum Elternbonus**: Eintragung im Portal statt Zettel, Frist 31. Juli, und dass nur bestätigte Stunden zählen (`soll-prozesse/14-elternbonus.md`, `schema/elternbonus-schema.sql`)
- [ ] Wird der **Elternbonus in Optigem als eigene Position** geführt, damit Aufschlag und Rückzahlung getrennt sichtbar sind (Geschäftsführung, Buchhaltung) — `schema/elternbonus-schema.sql`
- [ ] **Die Zuordnung der Fremdsysteme bestätigen und ergänzen** (Sekretariat, Buchhaltung, Admin): welche Änderung nach ASV-BW, welche nach Optigem, welche in den Tenant. Bis dahin korrigiert sie sich selbst — häufen sich bei einem System Aufgaben, die als *war nichts zu tun* abgehakt werden, ist die Zuordnung zu weit (`soll-prozesse/02-datenaenderung.md`)
- [ ] **Welche Bescheinigungen beim Abgang Routine sind** (Sekretariat) — Abgangszeugnis, Schulbescheinigung für die neue Schule, Bestätigung der Abmeldung? Entscheidet, was der Prozess anbietet und was außerhalb bleibt (`soll-prozesse/03-irregulaerer-abgang.md`)
- [ ] **Welche wiederkehrenden Aufgaben des Jahreswechsels in den drei Erinnerungen fehlen** (Sekretariat) — heute zieht der zweite Admin die Klassen von Hand um; was er dabei sonst noch tut, kennt nur er (`soll-prozesse/04-schuljahreswechsel.md`)
- [ ] **Was der Erhebungsbogen des Anmeldetags erhebt** und ob er künftig vorab im Portal ausgefüllt wird, statt Papier zu bleiben (Sekretariat, Schulleitung) — `soll-prozesse/06-anmeldetag.md`
- [ ] **Inhalt des Elternfragebogens der GS-Anmeldetag-Checkliste** (Sekretariat, Grundschulleitung) — unbekannt, könnte vorab digital laufen (`prozesse.md` Abschnitt 5.2, `grenzkarte.md`, Weiße Flecken)
- [ ] **Welche Excel-Listen darüber hinaus existieren** (Verwaltung, Hausdienstverwaltung, Hort) — laufend, keine Frist; jede gefundene ist entweder eine Abfrage auf den Bestand oder eine Lücke (`grenzkarte.md`, Weiße Flecken)
- [ ] **Die drei Preislisten eintragen** — die Tabellen stehen (Geschäftsführung pflegt sie danach selbst): Schulgeld je Schulart und Geschwisterrang (`tuition_fees`), Hortbeitrag je Modul und Tageszahl (`care_module_prices`), Mittagessen je Zahl der Esstage (`meal_prices`) — die Beträge liegen vor, die Struktur steht (`soll-prozesse/README.md`, `soll-prozesse/hebel.md`). Die Beitragssatzung braucht Weltenbaum nicht: Ermäßigungen darüber hinaus wendet die Buchhaltung in Optigem an (`soll-prozesse/09-hortvertrag.md`)
- [ ] **Die Werte im System eintragen** (`configured_values`, Geschäftsführung) — elf Codes; die Beträge und Stückzahlen liegen vor und stehen ausgeschrieben in `soll-prozesse/hebel.md` und im Kommentar an der Tabelle (`schema/querschnitt-schema.sql`): Freikauf und Strafe im Putzdienst, Bearbeitungs- und Anmeldegebühr, Änderungsgebühr der Betreuungsmodule, Geschwisterermäßigung, Kilometersatz und Meldegrenze der Rechnungsfreigabe, die drei des Elternbonus. Der Anfangsbestand der Anwendung setzt sie bewusst nicht: Jeder Wert trägt einen Gültigkeitstag, und den setzt, wer den Betrag verantwortet
- [ ] **Dependabot in den Repo-Einstellungen von `wb-backend` aktivieren** — `.github/dependabot.yml` steht (Docker- und Compose-Base-Images, monatlich), kein Konto und kein Token nötig; ohne die Aktivierung läuft die Datei ins Leere
- [ ] **Die zwei SharePoint-Bibliotheken einrichten** (zweiter Admin, Tenant-Admin-Zugriff): eine für die von Weltenbaum erzeugten Unterlagen samt Vorlagen-Ordner (App schreibt, Sekretariat und Geschäftsführung lesen **nur**), eine für die Schülerakte (Sekretariat und Geschäftsführung schreiben). Je Bibliothek ein `Sites.Selected`-Grant für die App-Registrierung. Form und Begründung stehen fest (`grenzkarte.md`, Q2; `project-parts.md` Abschnitt 10) — offen sind die konkreten Sites. Keine Berechtigung je Kind, Klasse oder Zweig: alle übrigen Rollen bekommen Dateien über Weltenbaum. Nötig vor Domäne 4, dazu einmalig die vorhandenen Aktenordner mit `child_file_folders` verknüpfen
- [ ] Klären, **warum die Geburtsurkunde kopiert wird**, wenn das Original ohnehin vorgezeigt wird (Sekretariat, Datenschutzbeauftragte:r) — Prüfung und Aufbewahrung sind zwei verschiedene Zwecke, und beim Masernnachweis hat die Schule sich bereits gegen die Kopie entschieden (`schema/gesundheit-schema.sql`). Bis zur Antwort bleibt die Kopie wie heute: ein nicht erhobener Scan ist nicht nachholbar, ein Weglassen später billig. Zusammen mit den Aufbewahrungsfristen unten zu entscheiden
- [ ] **Prozess für die DSGVO-Datenauskunft festlegen** (Schulleitung bzw. Datenschutzbeauftragte:r, Art. 15/16/20) — heute gibt es keinen: herausgegeben wird allein die digitale Schülerakte aus SharePoint (`prozesse.md` Abschnitt 17, `idea/06-dsgvo-organisatorisch.md`), die den Datenbankbestand nicht enthält. Nach dem Vollimport liegt der Personenbezug in vierzehn Schemata und ist über `persons.person_id` bzw. `children.child_id` vollständig einsammelbar — die Auskunft wird damit erstmals maschinell erzeugbar, wer sie in welcher Frist erteilt, ist trotzdem organisatorisch zu klären. Zusammen mit den Löschfristen unten zu entscheiden, gleicher Personenkreis
- [ ] Aufbewahrungsfristen für Schülerunterlagen nach baden-württembergischem Schulrecht klären (Schulleitung bzw. Datenschutzbeauftragte:r) — bestimmt, wie lange Stammdaten nach dem Abgang **behalten** werden müssen, bevor die Löschfrist überhaupt greifen darf (`idea/06-dsgvo-organisatorisch.md`). Muss stehen, bevor der Lösch-Job gebaut wird. Drei Teilfragen gehören dazu:
  - **Der Rücktritt vor dem ersten Schultag:** Der Lösch-Lauf zum 01.08. räumt Bewerbungen im Endstatus, **nimmt aber die mit abgeschlossenem Vertrag aus** — ein unterschriebener und gegengezeichneter Vertrag ist ein Rechtsdokument, auch wenn ihn niemand erfüllt hat (`schema/anmeldung-schema.sql`). Damit steht die Frage nicht mehr auf „mitlöschen oder nicht", sondern nur noch auf **wie lange behalten** — bis dahin bleiben diese Bewerbungen samt Vertrag liegen
  - **Ersetzte Verträge:** Wird ein abgeschlossener Vertrag neu aufgesetzt, bleibt der alte als Beleg stehen — wie lange, ist dieselbe Frage
  - **Die Verfahrensunterlagen am Kind** (Zeugnis, Grundschulempfehlung, Beobachtungsbogen): bewusst ohne eigene kurze Frist abgelegt, damit die Schule entscheidet und nicht der Entwurf

  Dazu **sechs Fristen je Entität**, die im Schema als `[?]` offenstehen und ohne die Block 17 nicht geschrieben werden kann. Jede ist eine eigene Antwort, keine folgt aus einer anderen:
  - **Bewerbungen ohne Aufnahme** — `schema/anmeldung-schema.sql`, `soll-prozesse/05-bewerbung.md`
  - **Ferienprogramm: Buchung und Kind nach dem letzten gebuchten Termin**, auch für schulfremde Kinder — `schema/ferien-schema.sql`, `soll-prozesse/10-ferienprogramm.md`
  - **Vertrags- und Zahlungsdaten** — `schema/stammdaten-schema.sql`, `soll-prozesse/03-irregulaerer-abgang.md`
  - **Daten ausgeschiedener Mitarbeitender** — `schema/stammdaten-schema.sql` und `schema/m365-schema.sql`, `soll-prozesse/00-zugang-und-portal.md`
  - **Eine versandte Mail, die an keiner Person mehr hängt** — `schema/querschnitt-schema.sql`; die einzige der sechs, die kein Soll-Block berührt
  - **Bestätigen, dass die übrigen Betriebsdaten keiner Aufbewahrungspflicht unterliegen** — `soll-prozesse/03-irregulaerer-abgang.md`

- [ ] **Wer den Lösch-Lauf anstößt und bestätigt** (Schulleitung und Datenschutzbeauftragte:r) — der Lauf löscht nicht von selbst, und wer ihn auslöst, ist nirgends benannt (`soll-prozesse/04-schuljahreswechsel.md`). Gehört in dasselbe Gespräch wie die Fristen darüber, vor Block 17

## Wiederkehrend / Ablauf-Termine

- [ ] Entra-ID Client-Secret der App-Registrierung „Clemens-Schule Weltenbaum" (trägt den App-only-Graph-Zugriff `Mail.Send`, nicht den Login — läuft es ab, bricht der OTP-Versand und damit der gesamte Elternzugang) läuft am **06.08.2028** ab — rechtzeitig vorher neues Secret erzeugen, in der Secrets-Datei im KeePass ersetzen, ausrollen und einmal deployen (`pipeline/runbook.md` Schritt 5)

## Später relevant, jetzt nicht klären

- AGs: nichts Konkretes bekannt, Zukunftsprojekt (`grenzkarte.md`, Weiße Flecken; `schema/ags-schema.sql` hält fest, dass es nichts zu bauen gibt). Fragen, wenn die Schulleitung von sich aus darauf kommt — vorher gibt es keine Grundlage
- Cyber-Versicherung: bei Jürgen rückbestätigen, dass die Bedingungen keine Verschlüsselung at rest fordern. Die vorliegenden Bedingungen enthalten dazu nichts — weder für den Host noch für Backups; der zweite Admin trägt den Verzicht auf Festplattenverschlüsselung mit (`pipeline/vps-repo/01-provisioning.md`). Reine Rückversicherung, kein offener Blocker
