# TODO — Organisatorische Vorbereitungen

Aufgaben, die reale Konten/Zugänge brauchen und die reine Konzept-Doku (`idea/`, `pipeline/`) nicht abdeckt — sortiert nach Dringlichkeit. Fachliche/technische Punkte, die eine Entwicklungs-Session selbst abarbeiten kann, stehen in `TODO-SESSIONS.md`.

## Bis Schulanfang September 2026 (Putzdienst als erste Fachdomäne, `fachdomaenen.md` Abschnitt 7)

- [x] DNS A/AAAA-Record für `api.clemens.schule` beim DNS-Provider der Schule (All-Inkl, KAS-Panel) auf die Server-IP setzen — `pipeline/runbook.md` Schritt 2, Voraussetzung für Caddys automatisches HTTPS und damit für den externen Zugriffsweg
- [x] Entra-ID-App-Registrierung im M365-Tenant der Schule anlegen (Tenant-Admin-Zugriff nötig, Tenant-Restriktion) — `pipeline/runbook.md` Schritt 5, zwingend da die Verwaltung den Putzdienst-Prozess intern startet und die Termine pflegt
- [ ] Redirect-URI in der bestehenden Entra-ID-App-Registrierung nachtragen (Tenant-Admin-Zugriff nötig) — zeigt auf die Origin des internen Frontends, nicht auf die API (`idea/04-identitaet-zugriff.md`) — setzbar erst, wenn dessen Hosting steht (`project-parts.md` Abschnitt 10), dieselbe Abhängigkeit wie die CORS-Policy
- [ ] Application Access Policy auf `noreply@clemens.schule` setzen (Tenant-Admin-Zugriff nötig): mail-enabled Security Group mit genau diesem Postfach, Policy darauf — ohne sie darf das App-only-`Mail.Send` im Namen jedes Postfachs im Tenant senden (`idea/04-identitaet-zugriff.md`). Trägt OTP-Versand, Buchungsbestätigung, Erinnerungen und Rundmail, muss damit vor dem Putzdienst-Buchungsfenster stehen
- [ ] NAS-Backup-Bootstrap auf der Synology (DSM-Zugriff nötig): SSH-Keypair für den Pull-Key generieren (privat ausschließlich auf dem NAS), Task-Scheduler-Job anlegen, VPS-Host-Key vorab in `known_hosts` pinnen (nach jedem `rebuild.sh` neu zu wiederholen) — `idea/05-backup-recovery.md`, muss vor den ersten echten Elterndaten laufen. Öffentlichen Pull-Key liefert der zweite Admin nach seiner Urlaubsrückkehr Ende August 2026
- [x] Neue Secrets im gemeinsamen KeePass ablegen: DB-Rollen-Passwörter (Runtime/Migration/Backup), Entra-ID Client-ID/Tenant-ID/Client-Secret, `age`-Verschlüsselungs-Passphrase — Feldschema folgt mit der `secrets.example.env`-Erweiterung im App-Stack-Repo
- [ ] Passwörter der enger geschnittenen DB-Rollen nachtragen, sobald `wb-backend/db/init-roles.sh` steht — Phase 2 schreibt jede Rolle aus dieser Datei auf den Host (`pipeline/vps-repo/02-hardening.md`), fehlt eine, startet der Stack unvollständig. Die Liste ist mit den zuletzt gebauten Domänen gewachsen und umfasst:
  - Konfession als Art.-9-Spalten: `children.denomination_id`/`congregation` **und** `guardians.denomination_id` (`domains/stammdaten.md`)
  - Bankverbindung: `payers.iban`/`bic`, nur Buchhaltung — sie überträgt sie einmal nach Optigem (`domains/stammdaten.md`, `glossar.md`)
  - Gesundheitsmerkmale, **zweistufig**: voller Satz für Verwaltung, Klassenlehrer:in und Hort gegen `health_traits.action_note` für alle unterrichtenden Personen — die Laufzeit-Rolle darf auf `health_traits` kein tabellenweites `SELECT`/`UPDATE` bekommen. Schreibend ist der Hinweis enger als lesend: `UPDATE (action_note)` bekommt allein die Klassenlehrer:in, die ihn formuliert (`domains/gesundheit.md`, `glossar.md`)
  - Bewertung der Bewerbung: `applications.fit_assessment_id`/`own_recommendation_id`/`rank_number`/`assessment_notes`, standardmäßig nicht breit sichtbar (`domains/anmeldung.md`)
  - Küchenprofil: eigene Rolle für Küche/Hausdienstverwaltung auf `meal_profiles`/`meal_profile_diets` (`domains/mensa.md`)
  - Straf-Aussetzung und Pflicht-Erlass: als Einziger dieser Liste eine **Schreib**beschränkung — `UPDATE` auf `cleaning_assignments.penalty_waived_at` und Schreibzugriff auf `cleaning_family_duties` nur für Geschäftsführung und Schulleitung. Gelesen werden beide weiter von der Laufzeit-Rolle: die Buchhaltung erkennt an `penalty_waived_at` die entfallende Forderung (`domains/grenzkarte.md`, Q3), Buchungsansicht und Solver brauchen `required_regular`/`required_major` — sonst sieht eine erlassene Familie die Standardpflicht. Eng gelesen wird allein `cleaning_family_duties.reason`, der nach außen nicht sichtbar sein darf (`domains/putzdienst.md`)
- [x] MFA auf dem healthchecks.io-Konto aktivieren (TOTP, Kontoeinstellungen) — `rules.md` Abschnitt 2; ohne das schaltet ein übernommenes Konto den Dead-Man's-Switch stumm, und Backup-Fehlschlag/Disk-Voll/fehlgeschlagener Patch-Lauf melden ausschließlich dorthin
- [ ] Stripe-Konto anlegen und AVV abschließen (stellt Stripe im Dashboard bereit) — trägt den Putzdienst-Freikauf und damit die erste Q3-Zahlung (`domains/grenzkarte.md`), muss vor den ersten echten Zahlungen stehen. Dabei zwei Punkte klären: welche Stripe-Gesellschaft Vertragspartner ist (EU-Sitz vs. Drittland-Transfer nach Art. 44 ff.) und welche Personendaten übertragen werden — Betrag und Referenz sind unvermeidlich, Name und E-Mail nicht zwingend (`idea/06-dsgvo-organisatorisch.md`)
- [ ] Zweiter Admin: GitHub-Username + SSH-Key einholen, vollen Zugriff auf GitHub-Org und VPS einrichten — für die Entwicklung unkritisch (alles in Git reproduzierbar), muss aber vor dem Produktivbetrieb mit echten Elterndaten stehen (`rules.md` Abschnitt 6). SSH-Key zugesagt für die Urlaubsrückkehr Ende August 2026
- [x] Vis365-Feldliste beim zweiten Admin holen: Vis365 → Schulverwaltungsimport → ASV-BW, die Anleitung stammt von ihm. Definiert, welche Felder der M365-Kontenexport braucht — zweite reale Datenquelle für das Stammdaten-Schema neben den Voranmeldeformularen (`domains/stammdaten.md`). Blockiert den Schema-Entwurf nicht, muss aber vor der Abnahme abgeglichen sein
- [ ] Stammdaten-Export für den Vollimport beschaffen (Ende August 2026 — derselbe Stichtag, ab dem der Freeze gilt, `domains/grenzkarte.md`). **Liegt beim Betreiber selbst**, keine Zulieferung durch Dritte und damit keine offene Fremdabhängigkeit — steht hier nur, damit die Frage „wer erzeugt diesen Export" nicht erneut als Blocker aufgemacht wird. Einzige Randbedingung: die Schema-Durchsicht unten liegt davor, sonst friert der Import einen ungeprüften Stand ein
- [ ] Stammdaten-Schema gemeinsam mit dem zweiten Admin durchgehen (von ihm angeboten) — nach seiner Urlaubsrückkehr Ende August 2026, vor dem ersten Import echter Daten. Läuft gegen den eingefrorenen Stand (`domains/grenzkarte.md`, „Freeze der Stammdaten") und ist damit die **letzte** Gelegenheit, eine bestehende Spalte noch zu ändern — danach ist jede Änderung eine Migration auf echten Personendaten
- [x] Klären, ob ASV-BW oder Optigem eine BIC brauchen — **Optigem verlangt sie für Konten außerhalb Deutschlands**, `payers.bic` bleibt damit und ist nicht streichbar (`domains/stammdaten.md`, „Zahlungsverantwortliche")
- [x] Ist das SEPA-Mandat an den Zahler oder an das Kind gebunden — **an das Kind**, je Kind wird ausdrücklich eines eingesammelt. Im Schema vor dem Freeze von `payers` nach `children` verlagert (`domains/stammdaten.md`, „Zahlungsverantwortliche")
- [ ] **Zweck der Voranmeldefelder Beruf, Konfession, Staatsangehörigkeit und Kirchengemeinde intern beschließen** (Schulleitung, Datenschutzbeauftragte:r). Die Spalten stehen inzwischen an `children` und `guardians` — entschieden, weil ein `DROP COLUMN` billig ist und ein beim Vollimport nicht erhobener Wert nicht nacherhebbar (`prozesse.md` Abschnitt 3.3). Damit ist die Aufgabe kleiner, aber nicht erledigt: Konfession ist ein Art.-9-Datum, und ein Feld ohne beschlossenen Zweck mit echten Personendaten zu füllen, schließt `rules.md` Abschnitt 7 aus. Die genannten Zwecke (Elternmitarbeit, christliche Familie, Wurzeln des Kindes) sind belastbar zu bestätigen, sonst fällt das Feld vor dem Import wieder raus. **Weiterhin vor dem Vollimport, nicht danach**

- [x] Benennen, **wer** den Erlass der Putzdienst-Pflicht und die Aussetzung der 45-€-Strafe auslösen darf — **Geschäftsführung und Schulleitung**, sonst niemand und nach außen unsichtbar (`domains/putzdienst.md`, „Erlass und Straf-Ausnahme"). Erster konkret benannter Fall der Rollendifferenzierung innerhalb des internen Personals; offen bleibt allein die Umsetzung als Spalten-GRANT bei der Rollenvergabe im Backend

## Unabhängig vom Putzdienst-Termin

- [x] Mensa-Anmeldeformular beim Sekretariat bzw. der Hausdienstverwaltung anfordern — **liegt vor** (`~/Downloads/25_26_RS_Mittagessen.pdf`, Realschule 25/26); Feldliste und Regeln ausgewertet in `prozesse.md` Abschnitt 9. Offen bleibt, ob ein Grundschul-Pendant existiert (`domains/grenzkarte.md`, „Weiße Flecken")
- [ ] `wb-backend`-Grundgerüst (Compose-Skeleton, FastAPI-Grundgerüst) ist nicht selbst geschrieben und noch nicht gegen `wb-backend/CLAUDE.md` durchgesehen
- [x] Wer den Dublettenhinweis an einer eingehenden Anmeldung abarbeitet („Verdacht auf vorhandenes Kind", `TODO-SESSIONS.md`): **das Sekretariat**. Der Hinweis hängt bewusst an der Bewerbung/Buchung, die ohnehin gesichtet wird, und nicht an einer eigenen Liste — er begleitet den Vorgang damit, statt ihn anzuhalten
- [x] Klären, ob §20 Abs. 9 IfSG eine Meldung ans Gesundheitsamt verlangt, wenn kein Masernnachweis vorgelegt wird — **ja**: die Infektionsschutz-Anlage des Betreuungsvertrags hält die Meldung ausdrücklich fest. Der Fall „geprüft, nicht vorgelegt, gemeldet" trägt `measles_proofs.reported_to_health_office_on` (`domains/gesundheit.md`)
- [ ] Nach dem Produktivstart klären, ob der **Hort eigenen Dateizugriff** bekommt (Hortleitung, Sekretariat) — vorerst führt er nichts Eigenes neben Hortverträgen und Ferienprogramm, seine Gesundheitsangaben bekommt er als Zeilen aus Weltenbaum (`domains/grenzkarte.md`, Q2)
- [ ] Beitragssatzung und aktuelle Preisliste des Horts (Preise je Modul und Mittagessen) bei Hortleitung/Geschäftsführung anfordern — ohne sie bleiben die Modulpreise bewusst außerhalb Weltenbaums (`domains/mensa.md`); nötig spätestens für den digitalen Hortvertrag (Domäne 2/4)
- [ ] **Die zwei SharePoint-Bibliotheken einrichten** (zweiter Admin, Tenant-Admin-Zugriff): eine für die von Weltenbaum erzeugten Unterlagen samt Vorlagen-Ordner (App schreibt, Sekretariat und Geschäftsführung lesen **nur**), eine für die Schülerakte (Sekretariat und Geschäftsführung schreiben). Je Bibliothek ein `Sites.Selected`-Grant für die App-Registrierung. Form und Begründung stehen fest (`domains/grenzkarte.md`, Q2; `project-parts.md` Abschnitt 10) — offen sind die konkreten Sites. Keine Berechtigung je Kind, Klasse oder Zweig: alle übrigen Rollen bekommen Dateien über Weltenbaum. Nötig vor Domäne 4, dazu einmalig die vorhandenen Aktenordner mit `child_file_folders` verknüpfen
- [ ] Klären, **warum die Geburtsurkunde kopiert wird**, wenn das Original ohnehin vorgezeigt wird (Sekretariat, Datenschutzbeauftragte:r) — Prüfung und Aufbewahrung sind zwei verschiedene Zwecke, und beim Masernnachweis hat die Schule sich bereits gegen die Kopie entschieden (`domains/gesundheit.md`). Bis zur Antwort bleibt die Kopie wie heute: ein nicht erhobener Scan ist nicht nachholbar, ein Weglassen später billig. Zusammen mit den Aufbewahrungsfristen unten zu entscheiden
- [ ] **Prozess für die DSGVO-Datenauskunft festlegen** (Schulleitung bzw. Datenschutzbeauftragte:r, Art. 15/16/20) — heute gibt es keinen: herausgegeben wird allein die digitale Schülerakte aus SharePoint (`prozesse.md` Abschnitt 17, `idea/06-dsgvo-organisatorisch.md`), die den Datenbankbestand nicht enthält. Nach dem Vollimport liegt der Personenbezug in sieben Schemata und ist über `persons.person_id` bzw. `children.child_id` vollständig einsammelbar — die Auskunft wird damit erstmals maschinell erzeugbar, wer sie in welcher Frist erteilt, ist trotzdem organisatorisch zu klären. Zusammen mit den Löschfristen unten zu entscheiden, gleicher Personenkreis
- [ ] Aufbewahrungsfristen für Schülerunterlagen nach baden-württembergischem Schulrecht klären (Schulleitung bzw. Datenschutzbeauftragte:r) — bestimmt, wie lange Stammdaten nach dem Abgang **behalten** werden müssen, bevor die Löschfrist überhaupt greifen darf (`idea/06-dsgvo-organisatorisch.md`). Muss stehen, bevor der Lösch-Job gebaut wird. Drei Teilfragen gehören dazu:
  - **Der Rücktritt vor dem ersten Schultag:** Der Lösch-Lauf zum 01.08. räumt Bewerbungen im Endstatus, **nimmt aber die mit abgeschlossenem Vertrag aus** — ein unterschriebener und gegengezeichneter Vertrag ist ein Rechtsdokument, auch wenn ihn niemand erfüllt hat (`domains/anmeldung.md`, „Löschung"). Damit steht die Frage nicht mehr auf „mitlöschen oder nicht", sondern nur noch auf **wie lange behalten** — bis dahin bleiben diese Bewerbungen samt Vertrag liegen
  - **Ersetzte Verträge:** Wird ein abgeschlossener Vertrag neu aufgesetzt, bleibt der alte als Beleg stehen — wie lange, ist dieselbe Frage
  - **Die Verfahrensunterlagen am Kind** (Zeugnis, Grundschulempfehlung, Beobachtungsbogen): bewusst ohne eigene kurze Frist abgelegt, damit die Schule entscheidet und nicht der Entwurf

## Wiederkehrend / Ablauf-Termine

- [ ] Entra-ID Client-Secret der App-Registrierung „Clemens-Schule Weltenbaum" (trägt den App-only-Graph-Zugriff `Mail.Send`, nicht den Login — läuft es ab, bricht der OTP-Versand und damit der gesamte Elternzugang) läuft am **06.08.2028** ab — rechtzeitig vorher neues Secret erzeugen, in der Secrets-Datei im KeePass ersetzen, ausrollen und einmal deployen (`pipeline/runbook.md` Schritt 5)

## Später relevant, jetzt nicht klären

- Cyber-Versicherung: bei Jürgen rückbestätigen, dass die Bedingungen keine Verschlüsselung at rest fordern. Die vorliegenden Bedingungen enthalten dazu nichts — weder für den Host noch für Backups; der zweite Admin trägt den Verzicht auf Festplattenverschlüsselung mit (`pipeline/vps-repo/01-provisioning.md`). Reine Rückversicherung, kein offener Blocker
